# 配置 / Flag 索引

_生成时间：2025-11-12T18:16:42+00:00_

## .env 变量

- `GOOGLE_PLACE_API` · .env · 存在值（未导出具体内容）
- `OPENAI_API_KEY` · .env · 存在值（未导出具体内容）
- `OPENAI_MODEL` · .env · 存在值（未导出具体内容）
- `BASE_URL` · .env · 存在值（未导出具体内容）
- `API_KEY` · .env · 存在值（未导出具体内容）
- `EXA_API_KEY` · .env · 存在值（未导出具体内容）
- `CONTEXT7_API_KEY` · .env · 存在值（未导出具体内容）
- `CONTEXT7_API_URL` · .env · 存在值（未导出具体内容）
- `CONTEXT7_MCP_URL` · .env · 存在值（未导出具体内容）
- `CHROMADB_HOST` · .env · 存在值（未导出具体内容）
- `CHROMADB_MODE` · .env · 存在值（未导出具体内容）
- `CHROMADB_DATA_PATH` · .env · 存在值（未导出具体内容）
- `CHROMADB_PORT` · .env · 存在值（未导出具体内容）
- `UVICORN_PORT` · .env · 存在值（未导出具体内容）
- `UVICORN_RELOAD` · .env · 存在值（未导出具体内容）
- `MYSQL_PORT` · .env · 存在值（未导出具体内容）
- `MYSQL_DATABASE` · .env · 存在值（未导出具体内容）
- `MYSQL_USER` · .env · 存在值（未导出具体内容）
- `MYSQL_HOST` · .env · 存在值（未导出具体内容）
- `MYSQL_PASSWORD` · .env · 存在值（未导出具体内容）
- `UVICORN_HOST` · .env · 存在值（未导出具体内容）
- `RABBITMQ_URL` · .env · 存在值（未导出具体内容）
- `RABBITMQ_EXCHANGE` · .env · 存在值（未导出具体内容）
- `RABBITMQ_EXCHANGE_TYPE` · .env · 存在值（未导出具体内容）
- `RABBITMQ_QUEUE` · .env · 存在值（未导出具体内容）
- `RABBITMQ_ROUTING_KEY` · .env · 存在值（未导出具体内容）
- `RABBITMQ_DLX` · .env · 存在值（未导出具体内容）
- `RABBITMQ_PREFETCH` · .env · 存在值（未导出具体内容）
- `RABBITMQ_QUEUE_MAX_LENGTH` · .env · 存在值（未导出具体内容）
- `RABBITMQ_DELIVERY_LIMIT` · .env · 存在值（未导出具体内容）
- `DOCKER_HOST_BRIDGE` · .env · 存在值（未导出具体内容）
- `RABBITMQ_HOST_OVERRIDE` · .env · 存在值（未导出具体内容）
- `REDIS_URL` · .env · 存在值（未导出具体内容）
- `REDIS_HOST_OVERRIDE` · .env · 存在值（未导出具体内容）
- `MONGODB_URI` · .env · 存在值（未导出具体内容）
- `MONGODB_DATABASE` · .env · 存在值（未导出具体内容）
- `MONGODB_HOST_OVERRIDE` · .env · 存在值（未导出具体内容）
- `MONGODB_REPLICA_SET_HOST` · .env · 存在值（未导出具体内容）
- `MONGODB_PORT` · .env · 存在值（未导出具体内容）
- `TELEGRAM_BOT_SECRETS` · .env · 存在值（未导出具体内容）
- `TELEGRAM_PROBE_MODE` · .env · 存在值（未导出具体内容）
- `TELEGRAM_PROXY_URL` · .env · 存在值（未导出具体内容）
- `TELEGRAM_PROXY_USER` · .env · 存在值（未导出具体内容）
- `TELEGRAM_PROXY_PASS` · .env · 存在值（未导出具体内容）
- `ngrokPublicUrl` · .env · 存在值（未导出具体内容）
- `TELEGRAM_BOT_PROFILE_PATH` · .env · 存在值（未导出具体内容）
- `TELEGRAM_BOT_TOKEN` · .env · 存在值（未导出具体内容）
- `TELEGRAM_BOT_NAME` · .env · 存在值（未导出具体内容）
- `TELEGRAM_BOT_USERNAME` · .env · 存在值（未导出具体内容）
- `USE_AGENTS` · .env · 存在值（未导出具体内容）
- `PORT` · .env · 存在值（未导出具体内容）
- `WEB_HOOK` · .env · 存在值（未导出具体内容）
- `TELEGRAM_TOKEN_SECRET` · .env · 存在值（未导出具体内容）
