# Proposal: Update Project Goal

## Background
现有 `openspec/project.md` 仍描述早期“Vue + Tailwind + 多页面 Agent 管理”目标，与最新 `.Learning/Up (researches).md` 中聚焦“后端工作流节点 GUI”方向不符。

## Why
- 统一项目的目标描述与研究成果，避免开发与文档偏离。
- 明确技术栈（Vue3 + Pinia + Element Plus + Codemirror / VueFlow）和核心模块（节点动作、模板管理、变量面板、日志）。
- 给前后端提供一致的术语、页面结构与交互约束。

## Scope
- 重写 `openspec/project.md`，覆盖：目标定位、关键模块、技术栈、架构示意、扩展建议。
- 引入研究文档中强调的功能块，并补充可行的增强（如组件库/图形库选择）。

## Out of Scope
- 不调整其他 spec、proposal；仅更新 project.md。
