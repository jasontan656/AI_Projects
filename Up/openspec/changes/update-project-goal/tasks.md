# Tasks

- [x] 审阅 .Learning/Up (researches).md 以提炼最新的工作流运维目标与模块划分。
- [x] 重写 openspec/project.md，明确 Vue 3 + Vite + Pinia + Element Plus + Codemirror + VueFlow 的核心栈。
- [x] 在文档中罗列 Nodes、Prompts、Workflow、Variables、Logs、Settings 模块及推荐的 SideNav + Tab 布局。
- [x] 记录使用的可视化/编辑增强组件（Element Plus 表单、VueFlow 画布、Codemirror 编辑器）及数据互通接口（Axios、WebSocket/SSE）。
- [x] 强化协作/合同约定（Chrome DevTools 回归、docs/contracts 快照同步、与 FastAPI 后端对齐）。
- [x] 运行 openspec validate update-project-goal --strict 确认格式与结构正确。
