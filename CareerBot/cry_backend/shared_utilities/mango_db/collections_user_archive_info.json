{
  "collection": "user_archive",
  "placeholder": true,
  "note": "占位说明：归档集合仅由连接器内部写入旧值快照，用于审计；外部模块不应直接写入。",
  "required": ["user_id"],
  "allow_extra": true,
  "fields": {
    "user_id": "string"
  },
  "allowed_ops": ["update.$set"]
}


