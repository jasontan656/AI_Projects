- date: '2025-10-20T17:39:12.3327646+08:00'
  user_prompt: |
    用户输入：我想要实现telegram机器人本地消息中转要写什么代码？，我有fastAPI，有ngrok。
  assistant_answer: |
    1. 输出独立 set_webhook 脚本，通过 python-telegram-bot 调用 Bot.set_webhook 并带 secret_token/allowed_updates/drop_pending_updates，匹配本地 /telegram/webhook。
    2. 指导以 ngrok 暴露 8000 端口并将 HTTPS 地址写入环境变量，然后运行脚本更新 Telegram Webhook。
- date: '$date'
  user_prompt: |
    APP.py的代码完成了，接着要干什么了？
  assistant_answer: |
    1. 提供 Pydantic 环境校验脚本，提醒在启动 FastAPI 之前先验证 `.env` 三个关键变量。
    2. 提供 get_webhook_info 自测脚本，并给出运行顺序（校验配置→启动 uvicorn→ngrok→setWebhook→验证）确保 webhook 指向最新 ngrok URL。
