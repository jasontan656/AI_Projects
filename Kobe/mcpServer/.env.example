# MCP Server HTTP 环境变量配置示例
# 将此文件复制为 .env 并填入实际值

# ============================================
# 鉴权配置
# ============================================
# 是否启用 Bearer Token 鉴权（默认: false）
MCP_AUTH_ENABLED=false

# Bearer Token（启用鉴权时必需）
# 客户端请求时需携带: Authorization: Bearer <token>
MCP_BEARER_TOKEN=your_secret_token_here

# ============================================
# 第三方服务 API Key
# ============================================
# OpenAI API Key（必需，用于 LangChain 调用）
OPENAI_API_KEY=sk-your-openai-key

# OpenAI 模型配置
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# Exa 搜索 API Key（可选，使用 exa_search 工具时需要）
EXA_API_KEY=sk-your-exa-key

# ============================================
# 日志配置
# ============================================
# 日志级别（DEBUG / INFO / WARNING / ERROR）
LOG_LEVEL=INFO

# ============================================
# 服务器配置
# ============================================
# 监听地址
HOST=0.0.0.0

# 监听端口
PORT=8000

# SSE 心跳间隔（秒）
SSE_HEARTBEAT_INTERVAL=30

